#include<stdio.h>
/*
void main() {
	int num[] = { 21,400,8,-3,77,99,-16,55,111,400 };
	int count = sizeof(num) / sizeof(num[0]);
	int temp;

	for (int x = 0; x < count; x++) {
		for (int y = 0; y < count - 1; y++) {
			if (num[y] > num[y+1]) {
				temp = num[y];
				num[y] = num[y + 1];
				num[y + 1] = temp;
			}
		}
	}

	printf("Ascending order number: ");
	for (int i = 0; i < count; i++) {
		printf("%d ", num[i]);
	}
	printf("\n");

	return 0;
	

}*/